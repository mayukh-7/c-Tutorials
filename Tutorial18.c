#include <stdio.h>

int main()
{
    int a = 2;
    float b = (float)54 / 5; // if we do operation between two integers the value will be a integer and the operation between 1 integer and 1 float the value will be a float value //
    printf("The value is %f\n ", b);
    return 0;
}
